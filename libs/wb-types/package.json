{
    "name": "@warbrokers/types",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "lint": "oxlint src",
        "typecheck": "tsc --noEmit",
        "test:unit": "vitest run",
        "vitest": "vitest",
        "clean": "rm -rf .turbo node_modules"
    },
    "dependencies": {
        "zod": "3.23.8"
    },
    "devDependencies": {
        "@warbrokers/typescript-config": "workspace:*",
        "typescript": "6.0.3",
        "vitest": "2.1.9"
    }
}
